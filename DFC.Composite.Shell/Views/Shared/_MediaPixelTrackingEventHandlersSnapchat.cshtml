@{
    var hostsAndCaptureEvents = new Dictionary<string, bool>
    {
        { "localhost", false },
        { "dev-beta.nationalcareersservice.org.uk", false },
        { "sit-beta.nationalcareersservice.org.uk", false },
        { "staging.nationalcareers.service.gov.uk", false },
        { "nationalcareers.service.gov.uk", false },
    };
    var key = Context.Request.Host.Host.ToLowerInvariant();
    hostsAndCaptureEvents.TryGetValue(key, out var captureEvents);

    if (captureEvents)
    {
        <script nws-csp-add-nonce="true">
            $('a[href^="/explore-your-education-and-training-choices/"]').click(function (e) {
                //snaptr('track', 'AD_CLICK');
                console.log(e);
            });
            $('a[href^="https://www.apprenticeships.gov.uk/apprentices"]').click(function (e) {
                //snaptr('track', 'AD_CLICK');
                console.log(e);
            });
            $('a[href^="https://www.tlevels.gov.uk/students/find"]').click(function (e) {
                //snaptr('track', 'AD_CLICK');
                console.log(e);
            });
            $('a[href^="https://www.findapprenticeship.service.gov.uk/traineeshipsearch"]').click(function (e) {
                //snaptr('track', 'AD_CLICK');
                console.log(e);
            });
            $('a[href^="https://www.ucas.com/"]').click(function (e) {
                //snaptr('track', 'AD_CLICK');
                console.log(e);
            });
        </script>
    }
}
